# PRD — 의학용어 단어암기 프로그램 (메디보카)
> 한 줄 정의: 의료 관련 학과 학생이 공공데이터 의학용어를 플래시카드·퀴즈로 외우는 로그인 없는 학습 웹   ·   모드: 간단

## ① 개요
의료 관련 학과 학생이 시험·실습에 필요한 의학용어를 짧은 시간에 반복 학습할 수 있는 웹 서비스다. 공공데이터 API에서 받아온 용어를 플래시카드로 넘겨 보며 외우고, 퀴즈로 스스로 확인한다. 회원가입 없이 바로 접속해 쓰는 것을 목표로 하며, 개인정보는 저장하지 않는다.

## ② 핵심가치
의학용어는 양이 많고 낯선 영문·한글이 섞여 있어 교재만으로는 반복 학습이 지루하다. 이 서비스는 용어를 카드 한 장 단위로 쪼개 "넘기고 → 확인하고 → 다시 보기"를 빠르게 돌려 준다. 기존 종이 단어장과 달리 검색으로 원하는 용어를 바로 찾고, 퀴즈로 자기 실력을 즉시 점검할 수 있다는 점이 다르다.

## ③ 타겟·시나리오
간호·응급구조·물리치료·임상병리 등 의료 관련 학과 1~2학년 학생이 주 사용자다. 스마트폰과 노트북에 익숙하고, 쉬는 시간이나 이동 중 짧게 보는 학습을 선호한다. 한 학생이 접속해 '순환기계' 주제를 골라 플래시카드 30장을 넘겨 보고, 헷갈린 용어는 검색으로 다시 찾아본 뒤, 퀴즈 10문제로 오늘 외운 내용을 점검하고 나간다.

## ④ 성공지표
핵심 지표: 한 명이 한 세션에서 플래시카드 20장 이상 넘겨 보거나 퀴즈 1회를 끝까지 완료한다. 리스크: **공공데이터에 바로 쓸 수 있는 '의학용어' API가 없거나 응답 형식이 예상과 다를 수 있다** — 이 경우 팀이 용어 목록을 CSV로 만들어 Supabase에 넣는 방식으로 대체한다.

## ⑤ 속성
- **category**: education
- **roles**: 학생(학습자) — 로그인/역할 구분 없는 단일 사용자
- **devices**: 모바일 웹, PC 웹 (둘 다 정상 동작 필요)

---

## 요구사항 개요
| ID | 요구사항 | 중요도 | 기능 수 |
|----|----------|--------|---------|
| R-1 | 용어 데이터 확보·조회 (공공데이터 API + Supabase) | high | 3 |
| R-2 | 플래시카드 학습 | high | 3 |
| R-3 | 퀴즈 | medium | 2 |
| R-4 | 나만의 복습 (즐겨찾기·오답노트) | low | 2 |
| R-5 | 공통 화면·반응형 | medium | 2 |

> 사용자가 고른 4가지 방식(플래시카드·퀴즈·검색·즐겨찾기/오답노트) 중, **팀 규칙(핵심 기능 1~3개)** 에 맞춰
> **플래시카드 + 검색을 코어(high)**, 퀴즈는 medium, 즐겨찾기/오답노트는 low(여유 있으면)로 우선순위를 나눴다.
> 즐겨찾기/오답노트는 로그인이 없으므로 **개인정보 없이 브라우저 localStorage**에만 저장한다.

## 권장 개발 순서
1. **먼저 만들 것(코어, high)** — F-1(API로 용어 불러오기) → F-2(용어 검색) → F-4(플래시카드 뒤집기).
   이 세 개가 "데이터가 뜨고 → 찾고 → 외운다"는 최소 흐름이자 첫 코딩 프롬프트가 된다.
2. **다음(medium)** — F-3(상세 보기), F-5·F-6(카드 섞기/안다·모른다), F-7·F-8(퀴즈), F-11·F-12(공통 UI·반응형).
3. **나중에(low)** — F-9(즐겨찾기), F-10(오답노트 자동 수집).

---

## 팀 작업 분리 제안 (바이브 코딩 협업 규칙 반영)
> `main` 직접 푸시 금지 · 각자 `feature/…` 브랜치 · PR로 병합 · 화면/컴포넌트 단위 분리

| 팀 역할 | 담당 요구사항/기능 | 예시 브랜치 |
|---------|-------------------|-------------|
| 프로젝트 리더 | 전체 구조·`App.tsx`·라우팅, PR 확인/병합, Vercel 배포 | `feature/main-page` |
| 데이터 담당 | R-1 (F-1 API 연결, `services/` , Supabase 시드) | `feature/public-api`, `feature/supabase` |
| 기능 담당 A | R-2 플래시카드 (F-4·F-5·F-6) | `feature/flashcard` |
| 기능 담당 B | R-3 퀴즈 (F-7·F-8) + F-2 검색 | `feature/quiz`, `feature/search` |
| 화면·테스트 담당 | R-5 공통 UI·반응형 (F-11·F-12), 오류 화면, 발표 준비 | `fix/mobile-layout` |

- API 키·Supabase 환경변수는 `.env.local`(→ `.gitignore`)에 두고 Vercel 환경변수로 등록. 채팅방에 키 올리지 않기.
- 커밋 메시지: `feat: 플래시카드 뒤집기 추가`, `feat: 공공데이터 API 연결`, `fix: 모바일 카드 잘림 수정` 형식.
